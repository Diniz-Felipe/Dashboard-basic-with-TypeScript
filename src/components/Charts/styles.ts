import styled from "styled-components";

// adicionar padding ao 1° elemento filho.

export const Charts = styled.div`
  grid-area: charts;
  min-width: 700px;
  
  background-color: #eee;
  border-radius: 1rem;
  padding: 1rem;
`;