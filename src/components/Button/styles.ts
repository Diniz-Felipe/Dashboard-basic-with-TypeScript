import styled from "styled-components";

export const Button = styled.button`
    padding: 10px;
    border-radius: 10px;
    border: none;
    cursor: pointer;
`;
export const Icon = styled.i`
    color: red;
`;